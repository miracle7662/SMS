import service from '../services/visitor.service.js';import { asyncHandler } from '../utils/async-handler.js';import { sendSuccess } from '../utils/api-response.js';const meta=req=>({ipAddress:req.ip,userAgent:req.get('user-agent')}),args=req=>[req.auth.activeSocietyId,req.auth.userId,req.auth.roles||[],req.auth.platformRoles||[]];export const listVisitors=asyncHandler(async(req,res)=>sendSuccess(res,200,'Visitors fetched successfully',await service.list(...args(req))));export const visitorOptions=asyncHandler(async(req,res)=>sendSuccess(res,200,'Visitor options fetched successfully',await service.options(...args(req))));export const getVisitor=asyncHandler(async(req,res)=>sendSuccess(res,200,'Visitor fetched successfully',await service.get(req.auth.activeSocietyId,Number(req.params.id),req.auth.userId,req.auth.roles||[],req.auth.platformRoles||[])));export const createVisitor=asyncHandler(async(req,res)=>sendSuccess(res,201,'Visitor gate entry created successfully',await service.create(req.auth.activeSocietyId,req.body,req.auth.userId,req.auth.roles||[],req.auth.platformRoles||[],meta(req))));export const approveVisitor=asyncHandler(async(req,res)=>sendSuccess(res,200,`Visitor ${req.body.decision.toLowerCase()} successfully`,await service.approve(req.auth.activeSocietyId,Number(req.params.id),req.body.decision,req.body.note,req.auth.userId,req.auth.roles||[],req.auth.platformRoles||[],meta(req))));export const gateVisitor=asyncHandler(async(req,res)=>sendSuccess(res,200,'Visitor gate status updated successfully',await service.gate(req.auth.activeSocietyId,Number(req.params.id),req.body.action,req.body.note,req.auth.userId,meta(req))));
