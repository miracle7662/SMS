import s from'../services/accounting.service.js';import{asyncHandler}from'../utils/async-handler.js';import{sendSuccess}from'../utils/api-response.js';export const data=asyncHandler(async(req,res)=>sendSuccess(res,200,'Accounting data fetched',await s.data(req.auth.activeSocietyId,req.auth.userId,req.query.from||`${new Date().getUTCFullYear()}-01-01`,req.query.to||new Date().toISOString().slice(0,10))));export const account=asyncHandler(async(req,res)=>sendSuccess(res,201,'Ledger account created',await s.account(req.auth.activeSocietyId,req.body,req.auth.userId)));export const voucher=asyncHandler(async(req,res)=>sendSuccess(res,201,'Voucher posted',await s.voucher(req.auth.activeSocietyId,req.body,req.auth.userId)));export const reverse=asyncHandler(async(req,res)=>sendSuccess(res,201,'Voucher reversed',await s.reverse(req.auth.activeSocietyId,Number(req.params.id),req.body,req.auth.userId)));
