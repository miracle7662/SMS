import s from'../services/society-document.service.js';import{asyncHandler}from'../utils/async-handler.js';import{sendSuccess}from'../utils/api-response.js';const roles=req=>[req.auth.roles||[],req.auth.platformRoles||[]];export const data=asyncHandler(async(req,res)=>sendSuccess(res,200,'Society documents fetched',await s.data(req.auth.activeSocietyId,req.auth.userId,...roles(req))));export const category=asyncHandler(async(req,res)=>sendSuccess(res,201,'Document category created',await s.category(req.auth.activeSocietyId,req.body,req.auth.userId)));export const upload=asyncHandler(async(req,res)=>sendSuccess(res,201,'Document uploaded',await s.upload(req.auth.activeSocietyId,req.body,req.auth.userId)));export const download=asyncHandler(async(req,res)=>{const x=await s.download(req.auth.activeSocietyId,Number(req.params.id),...roles(req));res.setHeader('Content-Type',x.document.mime_type);res.setHeader('Content-Disposition',`inline; filename="${encodeURIComponent(x.document.original_file_name)}"`);res.sendFile(x.filePath)});export const versions=asyncHandler(async(req,res)=>sendSuccess(res,200,'Document versions fetched',await s.versions(req.auth.activeSocietyId,Number(req.params.id),...roles(req))));export const version=asyncHandler(async(req,res)=>sendSuccess(res,201,'New document version uploaded',await s.version(req.auth.activeSocietyId,Number(req.params.id),req.body,req.auth.userId)));export const archive=asyncHandler(async(req,res)=>sendSuccess(res,200,'Document archived',await s.archive(req.auth.activeSocietyId,Number(req.params.id),req.auth.userId)));
