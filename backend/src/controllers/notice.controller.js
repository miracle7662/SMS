import service from '../services/notice.service.js';import { asyncHandler } from '../utils/async-handler.js';import { sendSuccess } from '../utils/api-response.js';const meta=req=>({ipAddress:req.ip,userAgent:req.get('user-agent')});
export const listNotices=asyncHandler(async(req,res)=>sendSuccess(res,200,'Notices fetched successfully',await service.list(req.auth.activeSocietyId)));export const noticeFeed=asyncHandler(async(req,res)=>sendSuccess(res,200,'Published notices fetched successfully',await service.feed(req.auth.activeSocietyId,req.auth.userId,req.auth.roles||[],req.auth.platformRoles||[])));export const createNotice=asyncHandler(async(req,res)=>sendSuccess(res,201,'Notice saved as draft',await service.create(req.auth.activeSocietyId,req.body,req.auth.userId,meta(req))));export const updateNotice=asyncHandler(async(req,res)=>sendSuccess(res,200,'Notice updated successfully',await service.update(req.auth.activeSocietyId,Number(req.params.id),req.body,req.auth.userId,meta(req))));export const publishNotice=asyncHandler(async(req,res)=>sendSuccess(res,200,'Notice published successfully',await service.changeStatus(req.auth.activeSocietyId,Number(req.params.id),'PUBLISHED',req.auth.userId,meta(req))));export const unpublishNotice=asyncHandler(async(req,res)=>sendSuccess(res,200,'Notice unpublished successfully',await service.changeStatus(req.auth.activeSocietyId,Number(req.params.id),'UNPUBLISHED',req.auth.userId,meta(req))));export const deleteNotice=asyncHandler(async(req,res)=>{await service.remove(req.auth.activeSocietyId,Number(req.params.id),req.auth.userId,meta(req));sendSuccess(res,200,'Notice deleted successfully',null)});
