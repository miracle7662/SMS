import service from '../services/complaint.service.js';import { asyncHandler } from '../utils/async-handler.js';import { sendSuccess } from '../utils/api-response.js';const meta=req=>({ipAddress:req.ip,userAgent:req.get('user-agent')}),ctx=req=>[req.auth.activeSocietyId,req.auth.userId,req.auth.roles||[],req.auth.platformRoles||[]];export const listComplaints=asyncHandler(async(req,res)=>sendSuccess(res,200,'Complaints fetched successfully',await service.list(...ctx(req))));export const complaintOptions=asyncHandler(async(req,res)=>sendSuccess(res,200,'Complaint options fetched successfully',await service.options(...ctx(req))));export const getComplaint=asyncHandler(async(req,res)=>sendSuccess(res,200,'Complaint fetched successfully',await service.get(req.auth.activeSocietyId,Number(req.params.id),req.auth.userId,req.auth.roles||[],req.auth.platformRoles||[])));export const createComplaint=asyncHandler(async(req,res)=>sendSuccess(res,201,'Complaint raised successfully',await service.create(req.auth.activeSocietyId,req.body,req.auth.userId,req.auth.roles||[],req.auth.platformRoles||[],meta(req))));export const updateComplaintWorkflow=asyncHandler(async(req,res)=>sendSuccess(res,200,'Complaint workflow updated successfully',await service.workflow(req.auth.activeSocietyId,Number(req.params.id),req.body,req.auth.userId,meta(req))));
