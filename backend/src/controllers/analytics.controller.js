import s from'../services/analytics.service.js';import{asyncHandler}from'../utils/async-handler.js';import{sendSuccess}from'../utils/api-response.js';export const data=asyncHandler(async(req,res)=>sendSuccess(res,200,'Society analytics fetched',await s.data(req.auth.activeSocietyId,req.query.from||`${new Date().getUTCFullYear()}-01-01`,req.query.to||new Date().toISOString().slice(0,10),req.auth.roles||[],req.auth.platformRoles||[])));
