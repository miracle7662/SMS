import express from'express';import{body,param,query}from'express-validator';import{account,data,reverse,voucher}from'../controllers/accounting.controller.js';import{authenticate}from'../middleware/authenticate.middleware.js';import{authorizePermissions,requireActiveSociety}from'../middleware/authorize.middleware.js';import{handleValidationErrors}from'../validators/auth.validators.js';const r=express.Router();r.use(authenticate,requireActiveSociety);r.get('/',authorizePermissions('society.accounting.view'),query('from').optional().isISO8601({strict:true}),query('to').optional().isISO8601({strict:true}),handleValidationErrors,data);r.post('/accounts',authorizePermissions('society.accounting.accounts'),body('account_code').trim().isLength({min:2,max:30}),body('account_name').trim().isLength({min:2,max:150}),body('account_type').isIn(['ASSET','LIABILITY','EQUITY','INCOME','EXPENSE']),body('account_group').isIn(['CASH','BANK','RECEIVABLE','PAYABLE','FUND','INCOME','EXPENSE','OTHER']),body('opening_balance').isFloat(),handleValidationErrors,account);r.post('/vouchers',authorizePermissions('society.accounting.post'),body('voucher_date').isISO8601({strict:true}),body('voucher_type').isIn(['RECEIPT','PAYMENT','CONTRA','JOURNAL']),body('debit_account_id').isInt({min:1}),body('credit_account_id').isInt({min:1}),body('amount').isFloat({gt:0}),body('reference_number').optional({checkFalsy:true}).isLength({max:100}),body('narration').trim().isLength({min:3,max:500}),handleValidationErrors,voucher);r.post('/vouchers/:id/reverse',authorizePermissions('society.accounting.reverse'),param('id').isInt({min:1}),body('note').trim().isLength({min:3,max:500}),handleValidationErrors,reverse);export default r;
