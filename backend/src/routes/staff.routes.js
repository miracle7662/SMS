import express from'express';import{body,param,query}from'express-validator';import{attendance,data,decision,pay,payroll,staff}from'../controllers/staff.controller.js';import{authenticate}from'../middleware/authenticate.middleware.js';import{authorizePermissions,requireActiveSociety}from'../middleware/authorize.middleware.js';import{handleValidationErrors}from'../validators/auth.validators.js';const r=express.Router();r.use(authenticate,requireActiveSociety);r.get('/',authorizePermissions('society.staff.view'),query('month').optional().matches(/^\d{4}-(0[1-9]|1[0-2])$/),handleValidationErrors,data);r.post('/',authorizePermissions('society.staff.manage'),body('full_name').trim().isLength({min:2,max:180}),body('mobile').trim().isLength({min:7,max:20}),body('email').optional({checkFalsy:true}).isEmail(),body('designation').trim().isLength({min:2,max:120}),body('employment_type').isIn(['FULL_TIME','PART_TIME','CONTRACT']),body('joining_date').isISO8601({strict:true}),body('monthly_basic').isFloat({min:0}),body('monthly_allowances').isFloat({min:0}),body('monthly_deductions').isFloat({min:0}),body('bank_name').optional({checkFalsy:true}).isLength({max:150}),body('account_number').optional({checkFalsy:true}).isLength({max:50}),body('ifsc_code').optional({checkFalsy:true}).isLength({max:20}),body('emergency_contact').optional({checkFalsy:true}).isLength({max:20}),body('address').optional({checkFalsy:true}).isLength({max:500}),handleValidationErrors,staff);r.post('/attendance',authorizePermissions('society.staff.attendance'),body('staff_id').isInt({min:1}),body('attendance_date').isISO8601({strict:true}),body('attendance_status').isIn(['PRESENT','ABSENT','HALF_DAY','PAID_LEAVE','UNPAID_LEAVE','WEEKLY_OFF','HOLIDAY']),body('check_in').optional({checkFalsy:true}).matches(/^([01]\d|2[0-3]):[0-5]\d$/),body('check_out').optional({checkFalsy:true}).matches(/^([01]\d|2[0-3]):[0-5]\d$/),body('notes').optional({checkFalsy:true}).isLength({max:300}),handleValidationErrors,attendance);r.post('/payroll',authorizePermissions('society.staff.payroll.generate'),body('staff_id').isInt({min:1}),body('payroll_month').matches(/^\d{4}-(0[1-9]|1[0-2])$/),handleValidationErrors,payroll);r.post('/payroll/:id/decision',authorizePermissions('society.staff.payroll.approve'),param('id').isInt({min:1}),body('status').isIn(['APPROVED','REJECTED']),body('note').optional({checkFalsy:true}).isLength({max:500}),handleValidationErrors,decision);r.post('/payroll/:id/pay',authorizePermissions('society.staff.payroll.pay'),param('id').isInt({min:1}),body('payment_date').isISO8601({strict:true}),body('payment_mode').isIn(['CASH','CHEQUE','NEFT','RTGS','UPI','BANK_TRANSFER']),body('payment_reference').optional({checkFalsy:true}).isLength({max:100}),handleValidationErrors,pay);export default r;
