import express from'express';import{body,param}from'express-validator';import{contract,data,decision,linkExpense,status,workOrder}from'../controllers/vendor-operation.controller.js';import{authenticate}from'../middleware/authenticate.middleware.js';import{authorizePermissions,requireActiveSociety}from'../middleware/authorize.middleware.js';import{handleValidationErrors}from'../validators/auth.validators.js';const r=express.Router();r.use(authenticate,requireActiveSociety);r.get('/',authorizePermissions('society.vendor_operations.view'),data);r.post('/contracts',authorizePermissions('society.vendor_contracts.manage'),body('vendor_id').isInt({min:1}),body('title').trim().isLength({min:3,max:180}),body('contract_type').isIn(['AMC','MONTHLY','QUARTERLY','YEARLY','ONE_TIME']),body('start_date').isISO8601({strict:true}),body('end_date').isISO8601({strict:true}),body('contract_amount').isFloat({min:0}),body('payment_frequency').isIn(['ONE_TIME','MONTHLY','QUARTERLY','HALF_YEARLY','YEARLY']),body('scope_of_work').trim().isLength({min:5,max:5000}),body('renewal_reminder_days').optional().isInt({min:0,max:365}),handleValidationErrors,contract);r.post('/work-orders',authorizePermissions('society.work_orders.manage'),body('vendor_id').isInt({min:1}),body('contract_id').optional({nullable:true}).isInt({min:1}),body('title').trim().isLength({min:3,max:180}),body('description').trim().isLength({min:5,max:5000}),body('priority').isIn(['LOW','MEDIUM','HIGH','URGENT']),body('requested_date').isISO8601({strict:true}),body('expected_completion_date').optional({checkFalsy:true}).isISO8601({strict:true}),body('estimated_amount').isFloat({min:0}),handleValidationErrors,workOrder);r.post('/work-orders/:id/decision',authorizePermissions('society.work_orders.approve'),param('id').isInt({min:1}),body('status').isIn(['APPROVED','REJECTED']),body('note').optional({checkFalsy:true}).isLength({max:500}),handleValidationErrors,decision);r.post('/work-orders/:id/status',authorizePermissions('society.work_orders.manage'),param('id').isInt({min:1}),body('status').isIn(['ASSIGNED','IN_PROGRESS','COMPLETED','CANCELLED']),body('completion_note').optional({checkFalsy:true}).isLength({max:1000}),body('final_amount').optional({nullable:true}).isFloat({min:0}),handleValidationErrors,status);r.post('/work-orders/:id/link-expense',authorizePermissions('society.work_orders.manage'),param('id').isInt({min:1}),body('expense_id').isInt({min:1}),handleValidationErrors,linkExpense);export default r;
