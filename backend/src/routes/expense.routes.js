import express from'express';import{body,param}from'express-validator';import{category,data,decision,expense,payment,vendor}from'../controllers/expense.controller.js';import{authenticate}from'../middleware/authenticate.middleware.js';import{authorizePermissions,requireActiveSociety}from'../middleware/authorize.middleware.js';import{handleValidationErrors}from'../validators/auth.validators.js';const r=express.Router();r.use(authenticate,requireActiveSociety);r.get('/',authorizePermissions('society.expenses.view'),data);r.post('/categories',authorizePermissions('society.expenses.manage'),body('name').trim().isLength({min:2,max:120}),body('description').optional({nullable:true}).isLength({max:300}),handleValidationErrors,category);r.post('/vendors',authorizePermissions('society.expenses.manage'),body('vendor_name').trim().isLength({min:2,max:180}),body('service_category').trim().isLength({min:2,max:120}),body('contact_person').optional({checkFalsy:true}).isLength({max:150}),body('mobile').optional({checkFalsy:true}).isLength({max:20}),body('email').optional({checkFalsy:true}).isEmail(),body('gstin').optional({checkFalsy:true}).isLength({max:20}),body('pan_number').optional({checkFalsy:true}).isLength({max:20}),body('address').optional({checkFalsy:true}).isLength({max:500}),handleValidationErrors,vendor);r.post('/',authorizePermissions('society.expenses.manage'),body('category_id').isInt({min:1}),body('vendor_id').optional({nullable:true}).isInt({min:1}),body('expense_date').isISO8601({strict:true}),body('invoice_number').optional({checkFalsy:true}).isLength({max:100}),body('description').trim().isLength({min:5,max:1000}),body('amount').isFloat({gt:0}),handleValidationErrors,expense);r.post('/:id/decision',authorizePermissions('society.expenses.approve'),param('id').isInt({min:1}),body('status').isIn(['APPROVED','REJECTED']),body('note').optional({checkFalsy:true}).isLength({max:500}),handleValidationErrors,decision);r.post('/:id/payments',authorizePermissions('society.expenses.pay'),param('id').isInt({min:1}),body('payment_date').isISO8601({strict:true}),body('amount').isFloat({gt:0}),body('payment_mode').isIn(['CASH','CHEQUE','NEFT','RTGS','UPI','CARD','ONLINE']),body('reference_number').optional({checkFalsy:true}).isLength({max:100}),body('notes').optional({checkFalsy:true}).isLength({max:500}),handleValidationErrors,payment);export default r;
