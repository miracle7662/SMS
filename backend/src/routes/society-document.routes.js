import express from'express';import{body,param}from'express-validator';import{archive,category,data,download,upload,version,versions}from'../controllers/society-document.controller.js';import{authenticate}from'../middleware/authenticate.middleware.js';import{authorizePermissions,requireActiveSociety}from'../middleware/authorize.middleware.js';import{handleValidationErrors}from'../validators/auth.validators.js';const r=express.Router(),id=param('id').isInt({min:1}),file=[body('file_name').trim().isLength({min:1,max:255}),body('mime_type').isIn(['application/pdf','image/jpeg','image/png','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet']),body('file_base64').isString().isLength({min:1,max:7500000})];r.use(authenticate,requireActiveSociety);r.get('/',authorizePermissions('society.documents.view'),data);r.post('/categories',authorizePermissions('society.documents.categories'),body('category_name').trim().isLength({min:2,max:120}),body('description').optional({checkFalsy:true}).isLength({max:300}),handleValidationErrors,category);r.post('/',authorizePermissions('society.documents.upload'),body('category_id').isInt({min:1}),body('title').trim().isLength({min:2,max:200}),body('document_number').optional({checkFalsy:true}).isLength({max:100}),body('description').optional({checkFalsy:true}).isLength({max:1000}),body('document_date').optional({checkFalsy:true}).isISO8601({strict:true}),body('expiry_date').optional({checkFalsy:true}).isISO8601({strict:true}),body('reminder_days').isInt({min:0,max:365}),body('visibility').isIn(['ALL_MEMBERS','COMMITTEE_ONLY','ADMIN_ONLY']),body('change_note').optional({checkFalsy:true}).isLength({max:500}),...file,handleValidationErrors,upload);r.get('/:id/download',authorizePermissions('society.documents.view'),id,handleValidationErrors,download);r.get('/:id/versions',authorizePermissions('society.documents.view'),id,handleValidationErrors,versions);r.post('/:id/versions',authorizePermissions('society.documents.upload'),id,body('change_note').optional({checkFalsy:true}).isLength({max:500}),...file,handleValidationErrors,version);r.post('/:id/archive',authorizePermissions('society.documents.archive'),id,handleValidationErrors,archive);export default r;
