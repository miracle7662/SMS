import express from'express';import{body,param}from'express-validator';import{allocateSlot,createSlot,createVehicle,parkingData,releaseSlot}from'../controllers/parking.controller.js';import{authenticate}from'../middleware/authenticate.middleware.js';import{authorizePermissions,requireActiveSociety}from'../middleware/authorize.middleware.js';import{handleValidationErrors}from'../validators/auth.validators.js';const router=express.Router();router.use(authenticate,requireActiveSociety);router.get('/',authorizePermissions('society.parking.view'),parkingData);router.post('/slots',authorizePermissions('society.parking.slots_manage'),body('slot_number').trim().isLength({min:1,max:50}),body('slot_type').isIn(['TWO_WHEELER','FOUR_WHEELER','BICYCLE','OTHER']),body('location').optional({nullable:true,checkFalsy:true}).isLength({max:150}),body('is_reserved').isBoolean(),handleValidationErrors,createSlot);router.post('/vehicles',authorizePermissions('society.parking.vehicles_manage'),body('flat_id').isInt({min:1}),body('vehicle_number').trim().isLength({min:4,max:30}),body('vehicle_type').isIn(['CAR','BIKE','SCOOTER','BICYCLE','OTHER']),body('brand').optional({nullable:true,checkFalsy:true}).isLength({max:100}),body('model').optional({nullable:true,checkFalsy:true}).isLength({max:100}),body('color').optional({nullable:true,checkFalsy:true}).isLength({max:50}),handleValidationErrors,createVehicle);router.post('/allocations',authorizePermissions('society.parking.allocate'),body('slot_id').isInt({min:1}),body('vehicle_id').isInt({min:1}),body('allocated_from').isISO8601({strict:true}),handleValidationErrors,allocateSlot);router.post('/allocations/:id/release',authorizePermissions('society.parking.allocate'),param('id').isInt({min:1}),handleValidationErrors,releaseSlot);export default router;
