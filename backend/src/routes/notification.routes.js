import express from'express';import{body,param}from'express-validator';import{campaign,data,dispatch,preferences,read,readAll,template}from'../controllers/notification.controller.js';import{authenticate}from'../middleware/authenticate.middleware.js';import{authorizePermissions,requireActiveSociety}from'../middleware/authorize.middleware.js';import{handleValidationErrors}from'../validators/auth.validators.js';const r=express.Router();r.use(authenticate,requireActiveSociety);r.get('/',authorizePermissions('society.notifications.view'),data);r.post('/templates',authorizePermissions('society.notifications.manage'),body('template_code').trim().isLength({min:2,max:50}),body('template_name').trim().isLength({min:2,max:150}),body('title_template').trim().isLength({min:2,max:200}),body('message_template').trim().isLength({min:2,max:2000}),body('default_channels').isArray({min:1}),body('default_channels.*').isIn(['IN_APP','EMAIL','SMS','WHATSAPP']),handleValidationErrors,template);r.post('/campaigns',authorizePermissions('society.notifications.manage'),body('title').trim().isLength({min:2,max:200}),body('message').trim().isLength({min:2,max:2000}),body('audience_type').isIn(['ALL_USERS','COMMITTEE','RESIDENTS','CUSTOM']),body('channels').isArray({min:1}),body('channels.*').isIn(['IN_APP','EMAIL','SMS','WHATSAPP']),body('user_ids').optional().isArray({max:500}),body('user_ids.*').optional().isInt({min:1}),handleValidationErrors,campaign);r.post('/dispatch',authorizePermissions('society.notifications.dispatch'),dispatch);r.patch('/read-all',authorizePermissions('society.notifications.view'),readAll);r.patch('/:id/read',authorizePermissions('society.notifications.view'),param('id').isInt({min:1}),handleValidationErrors,read);r.put('/preferences',authorizePermissions('society.notifications.view'),body('in_app_enabled').isBoolean(),body('email_enabled').isBoolean(),body('sms_enabled').isBoolean(),body('whatsapp_enabled').isBoolean(),handleValidationErrors,preferences);export default r;
