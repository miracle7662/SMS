import express from'express';import{body,param}from'express-validator';import{amenity,booking,cancel,data,decision,slot}from'../controllers/amenity.controller.js';import{authenticate}from'../middleware/authenticate.middleware.js';import{authorizePermissions,requireActiveSociety}from'../middleware/authorize.middleware.js';import{handleValidationErrors}from'../validators/auth.validators.js';const q=express.Router();q.use(authenticate,requireActiveSociety);q.get('/',authorizePermissions('society.amenities.view'),data);q.post('/',authorizePermissions('society.amenities.manage'),body('name').trim().isLength({min:2,max:150}),body('capacity').isInt({min:1}),body('is_bookable').isBoolean(),body('requires_approval').isBoolean(),body('hourly_charge').isFloat({min:0}),body('opening_time').optional({nullable:true}),body('closing_time').optional({nullable:true}),handleValidationErrors,amenity);q.post('/slots',authorizePermissions('society.amenities.manage'),body('amenity_id').isInt({min:1}),body('start_time').matches(/^\d{2}:\d{2}$/),body('end_time').matches(/^\d{2}:\d{2}$/),body('max_bookings').isInt({min:1}),body('charge_amount').isFloat({min:0}),handleValidationErrors,slot);q.post('/bookings',authorizePermissions('society.amenities.book'),body('slot_id').isInt({min:1}),body('flat_id').isInt({min:1}),body('booking_date').isISO8601({strict:true}),body('attendee_count').isInt({min:1}),handleValidationErrors,booking);q.post('/bookings/:id/decision',authorizePermissions('society.amenities.approve'),param('id').isInt({min:1}),body('status').isIn(['APPROVED','REJECTED']),handleValidationErrors,decision);q.post('/bookings/:id/cancel',authorizePermissions('society.amenities.book'),param('id').isInt({min:1}),handleValidationErrors,cancel);export default q;
